version: '3.8'

services:
  llm-api-router:
    build: .
    container_name: llm-api-router
    # 不再直接暴露到宿主机，统一走 nginx 前置
    expose:
      - "8000"
    volumes:
      # 持久化数据库（用户信息、路由配置、调用日志）
      - ./data/logs.db:/app/logs.db
      # 挂载配置文件，修改配置不需要重新构建镜像
      - ./config.yaml:/app/config.yaml:ro
    environment:
      # 时区设置，和服务器本地时间一致
      - TZ=Asia/Shanghai
      # 生产环境务必设置 JWT 密钥，长度须 >= 16（勿将含真实密钥的 compose 提交到仓库）
      # - LLM_ROUTER_JWT_SECRET=请替换为 openssl rand -hex 32 的输出
      # Token 有效天数（可选，默认 7）
      # - LLM_ROUTER_ACCESS_TOKEN_EXPIRE_DAYS=30
      # 敏感数据加密密钥（建议生产必填）：用户私有路由 API Key 用 Fernet 加密存储
      # 生成：openssl rand -base64 32；该密钥不可恢复，务必备份，丢失后存量 Key 无法解密
      # - ROUTER_DATA_KEY=请替换为 openssl rand -base64 32 的输出
      # 管理员用户名（可选）：设置后该用户自动获得 admin 角色；未设置时首个注册用户为管理员
      # - LLM_ROUTER_ADMIN_USERNAME=admin
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  nginx:
    image: nginx:alpine
    container_name: llm-api-router-nginx
    ports:
      - "8000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - llm-api-router
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"
